{
  "name": "acme-e2e",
  "private": true,
  "description": "End-to-end specs against the running compose stack (just up + just seed).",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
